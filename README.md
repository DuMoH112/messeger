# web-messeger django-react

#### Чтобы запустить проект необходимо выполнить комманду:
`$ sudo docker-compose up -d`


#### Чтобы остановить проект выполнить комманды:
```sh
$ sudo docker-compose stop
```